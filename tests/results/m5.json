{
  "milestone": 5,
  "revision": "the commit introducing this record",
  "date": "2026-09-08",
  "kernel": "Linux 7.2.2-1-cachyos x86_64",
  "rust": "1.98.1",
  "leancrypto": "1.8.0-1",
  "openssl": "3.6.4-1",
  "sqlite": "3.53.4-1",
  "docker": "29.7.2",
  "container_base": "archlinux@sha256:82b1b08faae9d61e3e7e13d562f4d09114d939105b0d59ff34140f3bd418593a",
  "package_snapshot": "2026/09/07",
  "schema": 5,
  "checks": [
    {"command": "cargo fmt --all", "result": "passed; stable fallback because nightly is unavailable"},
    {"command": "cargo clippy --workspace --locked --all-targets -- -D warnings", "result": "passed (default build)"},
    {"command": "cargo clippy --workspace --locked --all-targets --features pq-dev-harness,test-harness -- -D warnings", "result": "passed (dev-harness build)"},
    {"command": "cargo test --workspace --locked", "result": "all suites passed (pq 4+17+1+12+2 incl. 300-schedule property test, server 54 passed/1 explicitly ignored, client-core 12+1 real-API integration test, shared 8, wireguard-control 12 passed/1 explicitly ignored)"},
    {"command": "bash tests/run.sh unit", "result": "passed"},
    {"command": "bash tests/run.sh integration", "result": "passed"},
    {"command": "bash tests/run.sh docker-smoke (m2_management.sh + m3_exchange.sh + m4_smoke.sh + m4_crash.sh + m5_inactivity.sh)", "result": "passed: M2/M3/M4 scenarios unaffected; M5's inactivity scenario passes with real kernel peers"}
  ],
  "docker_scenario": {
    "topology": "1 server + peer-a + peer-b + peer-c on an isolated internal bridge, real production build, real --enable-pq-psk activation with --pq-psk-idle-timeout active",
    "real_kernel_constraint_found": "this codebase's server applies one hardcoded persistent-keepalive interval (shared::PERSISTENT_KEEPALIVE_INTERVAL_SECS, 25s) to every peer, with no per-peer override anywhere in the schema or CLI, so a genuinely keepalive-disabled, truly-idle tunnel cannot be produced with real containers today. The Docker scenario instead proves the achievable half (a healthy keepalive-carrying tunnel, with the idle timeout active, is never throttled, and rotates independently of other peers); the genuinely-idle pause/resume half is covered at the engine unit-test level, simulating sampled counters directly"
  },
  "limitations": [
    "a real-kernel demonstration of a genuinely idle tunnel pausing and then resuming promptly is not implemented: it needs a per-peer persistent-keepalive control this codebase does not yet have. Covered instead by pq/src/engine.rs's unit tests (idle_timeout_pauses_a_repeat_rotation_and_resumes_promptly_on_activity, idle_timeout_zero_never_pauses, a_relationship_with_no_activity_baseline_yet_is_never_paused), which simulate the sampled counters directly rather than needing a real kernel peer to actually go silent",
    "the numeric 300s/900s rotation/idle-timeout production defaults are not re-validated here; that is M9's job",
    "mixed strict/permissive/legacy scheduling interactions stay M6 territory",
    "no mixed-fleet/old-new binary compatibility, aarch64 execution, or external audit claim"
  ]
}
